---
title: "Z-1: Efficient Reinforcement Learning for Vision-Language-Action Models"
title_zh: "Z-1: 面向视觉-语言-动作模型的高效强化学习"
authors: "Lang Cao, Renhong Chen, Luyi Li, Peng Wang, Mofan Peng, Yitong Li"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31846v1"
tags: ["query:vla"]
score: 9.0
evidence: Z-1对VLA模型进行强化学习后训练以提升机器人操作
tldr: "现有视觉-语言-动作（VLA）模型多依赖行为克隆，无法从失败中改进。本文提出Z-1，基于π0.5的流匹配VLA模型，通过逐任务组相对策略优化（GRPO）进行强化学习后训练，结合共享前缀rollout、树结构轨迹分支和完成感知奖励校准。在24个RoboCasa任务上平均成功率80.6%，较监督微调提升13.2%，超越当前最优。表明系统GRPO后训练可显著提升VLA策略，无需额外私有演示。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31846v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1411, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31846v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1376, \"height\": 478, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31846v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31846v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 437, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31846v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 965, \"label\": \"Table\"}]"
motivation: 现有VLA策略基于行为克隆或监督微调，缺乏从自身失败中学习的机会，限制性能提升。
method: Z-1框架在π0.5模型上先进行监督微调，再应用逐任务GRPO策略，并采用共享前缀rollout、树结构分支、完成感知奖励校准及联合训练。
result: "在24个RoboCasa任务上平均成功率80.6%，比监督微调基线提升13.2%，超越现有最佳模型。"
conclusion: 系统GRPO后训练能有效提升流匹配VLA模型的策略性能，无需额外私有数据集。
---

## 摘要
视觉-语言-动作（VLA）模型通过连接语言指令、视觉观测和连续控制，为机器人操作提供了一个有前景的框架。然而，大多数现有策略仍受限于行为克隆或基于固定演示的监督微调（SFT），这限制了策略从自身失败中改进的机会。在本文中，我们提出了Z-1，一个面向基于流的VLA模型的强化学习（RL）后训练框架。Z-1基于π_{0.5}构建，仅使用公开的RoboCasa演示进行SFT，然后在24个标准RoboCasa任务上应用逐任务分组相对策略优化（GRPO）策略。为提高在线优化的效率和稳定性，Z-1结合了共享前缀轨迹构建、树结构轨迹分支、完成感知奖励校准以及VLM与动作专家的选择性联合训练。在所有24个RoboCasa任务中，Z-1实现了80.6%的平均成功率，相比其SFT初始化提升了13.2个百分点，并超越了已发表的SOTA模型。这些结果表明，系统性的GRPO后训练可以在无需额外私有演示的情况下显著改进基于流的VLA策略。

## Abstract
Vision-Language-Action (VLA) models offer a promising framework for robotic manipulation by connecting language instructions, visual observations, and continuous control. However, most existing policies remain limited by behavior cloning or supervised fine-tuning (SFT) from fixed demonstrations, which provides limited opportunity to improve from the policy's own failures. In this paper, we present Z-1, a reinforcement learning (RL) post-training framework for flow-based VLA models. Built on top of $π_{0.5}$, Z-1 uses only publicly released RoboCasa demonstrations for SFT and then applies a task-wise Group Relative Policy Optimization (GRPO) strategy across $24$ standard RoboCasa tasks. To improve the efficiency and stability of online optimization, Z-1 combines shared-prefix rollout construction, tree-structured trajectory branching, completion-aware reward calibration, and selective joint training of VLM and Action Expert. Across all $24$ RoboCasa tasks, Z-1 achieves an average success rate of $80.6\%$, improving over its SFT initialization by $13.2\%$ points and outperforms the published sota models. These results show that systematic GRPO post-training can substantially improve flow-based VLA policies without additional private demonstrations.

---

## 论文详细总结（自动生成）

### 论文中文详细总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：现有的视觉-语言-动作（VLA）模型（如RT-2、OpenVLA、π₀、π₀.₅）大多依赖行为克隆或监督微调（SFT）从固定演示中学习策略，这种方法无法让策略从自身失败中改进。在长时域和组合性任务中，微小预测误差会累积，导致机器人进入训练数据覆盖不足的状态，限制了性能上限。
- **核心问题**：如何在不依赖额外私有演示的条件下，通过在线强化学习后训练提升基于流匹配的VLA模型的性能，同时解决 rollout 生成昂贵、稀疏奖励带来高方差、以及冻结视觉-语言主干限制感知-动作协同适应等挑战。
- **整体含义**：提出一种系统化的模块化 GRPO 后训练框架 Z-1，证明仅使用公开数据即可显著提升流匹配 VLA 策略，为实际机器人操作提供了更高效的训练范式。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：基于 π₀.₅ 模型，先进行场景级监督微调（SFT），再对每个任务应用任务级 GRPO 后训练。通过多项创新设计提升在线优化的效率、稳定性和适应性。
- **关键技术细节**：

| 模块 | 功能描述 |
|------|----------|
| **共享前缀 GRPO** | 在同一个 GRPO 组内，让所有轨迹共享一段公共前缀（approach 阶段），从同一状态出发执行独立后缀。前缀部分不参与梯度更新，减少冗余计算，使奖励比较聚焦于关键操作阶段。 |
| **树结构前缀分支** | 在共享前缀基础上引入层次化分支点，形成轨迹树。只有完全共享的根段被掩码，部分共享段仍可训练，从而保留更多可学习信号，同时保持比较一致性。 |
| **完成感知奖励校准** | 对成功轨迹按其完成步数进行指数衰减校准：`˜r = r·γ^{d_i}`（γ=0.998）。在全员成功的组中使用非负最小基线（成功轨迹获得正优势），避免惩罚慢但成功的轨迹，从而鼓励更快完成而不损害可靠性。 |
| **选择性 VLM-动作专家联合训练** | 默认只训练动作专家；仅当训练诊断（如SFT成功率低、AE-only GRPO进展慢）表明需要时，才联合训练 VLM 主干和动作专家，实现感知与动作的协同适应。 |

- **公式与算法流程**（文字说明）：
  - 采用分组相对策略优化（GRPO），每组采样 G 个轨迹，计算组内相对优势 `A_i = (R_i - μ_R) / (σ_R + ε)`。
  - 优化目标为截断的组相对目标：`L_GRPO(θ) = - (1/∑|B_i|) * ∑_{i} ∑_{(x,a)∈B_i} min(ρ_θ(x,a)A_i, clip(ρ_θ(x,a), 1-η, 1+η)A_i)`，其中 ρ 为重要性采样比（通过流-SDE 转换计算对数概率）。
  - 前缀部分排除在可训练集 B_i 之外；树结构分支中只有完全共享根段被排除。

#### 3. 实验设计：数据集/场景、benchmark、对比方法
- **数据集与场景**：使用公开的 **RoboCasa** 仿真家用操作基准中的 **24 个标准任务**，涵盖门、抽屉、拾取放置、微波炉、水槽/水龙头、炉灶、咖啡制作等场景。训练数据仅包括 RoboCasa 公开的 1,199 条演示。
- **Benchmark**：在 24 个任务上以任务成功率为评价指标，报告类别级和整体平均成功率。
- **对比方法**：与已发表的四个 RoboCasa 基线进行比较——**GR00T、GR00T N1.5、Video Policy、X-WAM**（结果取自原论文）。同时与自身 SFT 初始化（Z-1 SFT）对比。

#### 4. 资源与算力
- **文中未明确说明使用的 GPU 型号、数量或训练时长**。仅提及 SFT 基于 OpenPI JAX 训练代码，GRPO 基于 RLinf 框架，使用 AdamW 优化器（学习率 5e-6）、全局批大小 1024、微批大小 16、默认组大小 8、4 个 rollout epoch、64 个并行训练环境。未提供具体硬件配置和总训练时间。

#### 5. 实验数量与充分性
- **实验数量**：主实验在 24 个任务上评估成功率；另外进行了三组消融实验：（1）共享前缀与树结构前缀对比（在 TurnOnSinkFaucet 任务上）；（2）选择性联合训练 vs AE-only（在 TurnOnStove 任务上）；（3）完成感知奖励衰减 vs 标准 GRPO 与奖励惩罚变体（在 PnPStoveToCounter 任务上）。消融实验均附有训练曲线（成功率与策略损失）。
- **充分性与公平性**：
  - 消融实验覆盖了提出的每个关键模块，并提供了可视化对比。
  - 训练诊断用于选择模块配置，且固定于最终评估之前，未使用最终评估 rollout 进行调参，避免了信息泄漏。
  - 对比基线数据取自原论文，非完全复现，但作者已指出这是“参考”而非受控复现，在公平性上略有局限。
  - 每个任务评估使用独立 rollout（SFT 用 48 次，RL 用 64 次），统计可靠。
  - 消融实验仅在单个任务上进行，是否具有跨任务泛化性有待更多验证。

#### 6. 论文的主要结论与发现
- **主要结论**：系统性的 GRPO 后训练可以显著提升基于流的 VLA 策略，无需额外私有演示。
- **具体发现**：
  - Z-1 RL 在 24 个任务上平均成功率为 **80.6%**，比 Z-1 SFT（67.4%）提升 **13.2 个百分点**，超越所有对比基线（包括 X-WAM 的 79.2%）。
  - 改进最显著的类别是接触密集型任务：抽屉（83.4% → 96.1%）、水槽/水龙头（63.2% → 94.3%）、门（93.2% → 97.0%）、拾取放置（53.9% → 70.9%）。
  - 消融表明：共享前缀和树结构前缀均优于标准 GRPO；选择性联合训练在炉灶任务上显著优于仅训练动作专家；完成感知奖励衰减比奖励惩罚更稳定有效。
  - 炉灶任务仍然具有挑战性，Z-1 RL 虽优于 SFT 但落后于 X-WAM，暗示需要更强的表征适应或探索机制。

#### 7. 优点：方法或实验设计上的亮点
- **方法创新**：
  - 提出共享前缀和树结构分支，巧妙减少 rollout 冗余并聚焦关键决策段。
  - 完成感知奖励校准在无额外奖励模型或密集奖励的情况下提供更精细的组内反馈。
  - 选择性联合训练避免盲目更新 VLM，节省计算并保持稳定性。
  - 所有模块正交且可组合，基于训练诊断动态选择，实用性强。
- **实验设计**：
  - 在 24 个多样化任务上评估，涵盖多种操作类型，结果全面。
  - 消融实验针对每个关键模块独立验证，并报告训练动态曲线，结论可靠。
  - 仅使用公开演示，与基线对比时强调这一限制，体现了公平比较意识。

#### 8. 不足与局限
- **实验覆盖局限**：消融实验仅在各别任务上执行（如 TurnOnSinkFaucet、TurnOnStove、PnPStoveToCounter），未在全部 24 个任务上验证模块通用性，可能受任务特性影响。
- **比较公平性**：基线结果取自原论文，可能由于不同评估设置（seed 数、evaluation rollout 数、超参）导致偏差，非完全受控复现。
- **资源信息缺失**：未报告 GPU 型号、数量、训练时间，使计算成本难以评估。
- **任务局限性**：在炉灶类任务上仍低于 X-WAM，且失败模式未深入分析；长时域运输、空间推理和中间恢复能力仍不足。
- **依赖仿真环境**：实验仅在 RoboCasa 仿真中进行，未验证在真实机器人上的迁移性。
- **模块选择依赖人工诊断**：训练诊断（如 SFT 成功率、早期 GRPO 进展）缺乏自动化阈值，可能引入人为偏差。
- **未与其他 RL 后训练方法直接对比**：仅对比了 SFT 和已完成发布的基线，未与 πRL、ReinFlow 等近期 RL 后训练工作在相同设置下比较。

（完）
