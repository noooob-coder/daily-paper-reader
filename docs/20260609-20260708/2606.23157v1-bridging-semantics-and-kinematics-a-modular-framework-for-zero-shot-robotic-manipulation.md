---
title: "Bridging Semantics and Kinematics: A Modular Framework for Zero-Shot Robotic Manipulation"
title_zh: 桥接语义与运动学：用于零样本机器人操作的模块化框架
authors: "Ali Alabbas, Dipshikha Das, Camillo Murgia, Sainul Ansary, Alaa Elkamash, Philip Long"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23157v1"
tags: ["query:vla"]
score: 10.0
evidence: 使用视觉语言模型和语义路由器的语言引导机器人操作
tldr: "现有零样本机器人操作难以在语言指令与底层运动之间建立可靠联系，且视觉语言模型存在空间模糊和语义幻觉。该文提出模块化无训练框架，通过FastSAM和SoM提示生成锚点，将VLM降级为LLM作为语义路由器，再由Task Orchestrator调用MTC生成无碰撞轨迹。在开放世界顺序操作和空间推理任务上实现62%端到端成功率，无需领域训练或手动编程。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23157v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23157v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23157v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 864, \"height\": 506, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23157v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23157v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 131, \"label\": \"Table\"}]"
motivation: 解决零样本操作中高层语义理解与底层运动控制之间的鸿沟，消除标准VLM的空间歧义和语义幻觉。
method: 分解为视觉感知、语义解释和任务执行三阶段，使用FastSAM和SoM生成接地锚点，VLM仅作为LLM进行语义路由，MTC规划轨迹。
result: "在两个零样本实验中达到62%的端到端任务成功率，无需领域特定训练或手动坐标编程。"
conclusion: 该模块化框架有效桥接语义与运动学，实现了零样本语言引导的可靠操作，具有良好的泛化性和可重构性。
---

## 摘要
本文提出了一种模块化免训练框架，用于在半结构化环境中进行零样本、语言引导的机器人操作。该架构通过将视觉-动作流水线分解为三个阶段：视觉感知、语义解释和任务执行，弥合了高层次推理与低层次运动学之间的鸿沟。为了克服标准视觉-语言模型（VLM）固有的空间模糊性和语义幻觉，感知模块采用FastSAM和Set-of-Mark（SoM）提示，动态生成接地、字母数字的视觉锚点。然后，同一基础模型纯粹作为大型语言模型（LLM）充当语义路由器，将无约束的人类指令转化为可验证、可重配置的配置。最后，任务编排器将这些配置动态解析为MoveIt任务构造器（MTC），以生成无碰撞轨迹。该框架在两个零样本实验设置中进行了评估：无约束的开放世界顺序操作和密集关系空间推理，在两种场景下实现了62%的端到端任务成功率，证明了其在无需特定领域训练或手动坐标编程的情况下可靠执行复杂物理动作的能力。

## Abstract
This paper presents a modular training-free framework for zero-shot, language-guided robotic manipulation in semi-structured environments. The architecture bridges the gap between high-level reasoning and low-level kinematics by decomposing the vision-action pipeline into three stages: visual perception, semantic interpretation, and task execution. To overcome the spatial ambiguity and semantic hallucinations inherent in standard Vision-Language Models (VLMs), the perception module employs FastSAM and Set-of-Mark (SoM) prompting to dynamically generate grounded, alphanumeric visual anchors. The same foundation model then operates purely as a Large Language Model (LLM) to act as a semantic router, translating unconstrained human directives into verifiable, reconfigurable configurations. Finally, these configurations are dynamically parsed by a Task Orchestrator into MoveIt Task Constructor (MTC) to generate collision-free trajectories. The framework is evaluated across two zero-shot experimental setups: unconstrained open-world sequential manipulation and dense relational spatial reasoning, achieving a 62% end-to-end task success rate across both scenarios, demonstrating its capacity to reliably execute complex physical actions without domain-specific training or manual coordinate programming.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现有零样本机器人操作难以在高层语言指令与底层运动控制之间建立可靠联系；标准视觉-语言模型（VLM）在空间推理中存在**空间模糊性**和**语义幻觉**，导致指令执行失败。
- **整体含义**：本文旨在弥合语义理解与运动学执行之间的鸿沟，构建一个无需领域训练或手动编程的模块化框架，使得机器人能够根据自然语言指令在**半结构化环境**中完成复杂的物理操作任务。

## 2. 方法论：核心思想、关键技术细节、算法流程

- **核心思想**：将视觉-动作流水线分解为三个独立阶段——**视觉感知**、**语义解释**和**任务执行**，每个阶段使用现成模型（免训练），通过中间表征（接地锚点、可验证配置）连接。
- **关键技术细节**：
  - **视觉感知**：使用 **FastSAM** 进行实例分割，然后采用 **Set-of-Mark (SoM) 提示**，为每个物体动态生成带有字母数字标签的视觉锚点。
  - **语义解释**：将同一个基础 VLM（例如 GPT-4V）**降级为纯大型语言模型 (LLM)**，作为“语义路由器”。它接收人类指令和锚点列表，输出**可验证、可重配置的任务配置**（例如“将A放到B上”）而非直接生成运动命令。
  - **任务执行**：**Task Orchestrator**（任务编排器）将语义路由器输出的配置动态解析为 **MoveIt Task Constructor (MTC)** 的规划指令，生成**无碰撞轨迹**并控制机器人执行。
- **算法流程**（文字说明）：
  1. 输入：场景图像 + 自然语言指令。
  2. 视觉感知模块对图像进行实例分割，并为每个物体赋予唯一锚点标签。
  3. 语义路由器（LLM）基于指令和锚点标签，推理出目标物体、操作关系、顺序等，输出结构化配置。
  4. 任务编排器将配置转换为 MTC 可执行的运动规划任务，调用运动学求解器生成轨迹并执行。
- **关键特性**：全程**免训练**，不更新模型参数；框架可重构，支持不同物体和指令组合。

## 3. 实验设计：场景、基准、对比方法

- **实验场景**：两个零样本实验设置：
  - **无约束的开放世界顺序操作**：例如“先拿起红色杯子，再把它放到蓝色托盘上”等多步操作。
  - **密集关系空间推理**：例如具有复杂空间关系的任务（如“将盒子放在圆柱体的左边”）。
- **基准与指标**：评估**端到端任务成功率**（即机器人完全正确执行指令的比例），无其他辅助指标。
- **对比方法**：文中**未提及任何对比基线方法**，仅报告了框架自身的成功率。

## 4. 资源与算力（若未说明则指出）

- 文中**未明确给出**使用的 GPU 型号、数量、训练时长等信息。由于框架是**免训练**，推测仅在推理阶段使用了预训练的 FastSAM 和 VLM/LLM 模型（可能依赖云端 API 或本地 GPU），但具体算力消耗未披露。

## 5. 实验数量与充分性

- **实验数量**：总共进行**两组**实验（两个不同场景），每组实验可能包含多个任务实例（具体数量未说明），最终报告综合成功率为 **62%**。
- **充分性评价**：
  - **不足**：缺乏消融实验（如去掉 SoM 或语义路由器的影响）、未与现有零样本方法（如 RT-2、SayCan 等）定量对比，实验设计不够全面。
  - **客观公平性**：未给出任务的具体难度分布、失败案例分析，可能存在选择偏差。但两个场景覆盖了顺序操作和空间推理，有一定代表性。

## 6. 论文的主要结论与发现

- 提出的模块化免训练框架能够有效**桥接语义与运动学**，在**零样本**条件下实现 **62% 的端到端任务成功率**。
- 证明了通过**动态接地锚点**（FastSAM + SoM）和**VLM降级为LLM**的策略可以缓解标准 VLM 的空间模糊和语义幻觉问题。
- 框架无需领域训练或手动坐标编程，具有良好的**泛化性**和**可重构性**，可适用于不同物体和任务组合。

## 7. 优点：方法或实验设计上的亮点

- **模块化架构**：将感知、语义、执行彻底解耦，各模块可独立替换升级，增强了可解释性和工程可行性。
- **免训练**：直接利用已有的视觉基础模型和 LLM，无需收集机器人操作数据，降低了应用门槛。
- **利用 SoM 锚点**：通过字母数字标签将视觉空间与语义空间对齐，有效消除 VLM 直接输出坐标时的歧义。
- **语义路由器设计**：将 VLM 限制为纯语言推理，避免了视觉-语言混合带来的幻觉，同时输出可验证的配置而非原始坐标，增加了安全性和可控性。

## 8. 不足与局限：实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：仅有两个场景，任务类型和复杂度有限；未测试动态环境、遮挡、光照变化等挑战。
- **成功率较低**（62%），表明仍有相当比例的失败案例（如物体分割失败、语义误解、规划碰撞等），未提供失败模式分析。
- **缺乏对比基线**：无法定量评估本框架相对于现有零样本操作方法的优劣。
- **依赖外部模型**：框架性能受限于 FastSAM 的分割质量和底层 LLM 的推理能力，这些模型本身存在偏差风险。
- **应用限制**：半结构化环境假设要求场景相对固定，物体大致可分割；对于细微操纵（如拧螺丝）或非刚性物体可能效果不佳。

（完）
