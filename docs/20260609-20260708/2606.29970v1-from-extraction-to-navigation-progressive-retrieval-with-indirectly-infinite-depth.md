---
title: "From Extraction to Navigation: Progressive Retrieval with Indirectly Infinite Depth"
title_zh: 从提取到导航：渐进式检索与间接无限深度
authors: "Linxiao Che, Shanshan Huang, Haitao Lu, Yijia Sun, Qiang Luo, Ruiming Tang, Han Li, Kun Gai, Guorui Zhou"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29970v1"
tags: ["query:recsys"]
score: 10.0
evidence: 将大规模推荐检索建模为图导航
tldr: 大规模推荐检索正从静态相似匹配转向动态导航，但现有方法存在兴趣隧道和搜索漂移。提出IID-Nav框架，通过目标感知导航策略替代被动邻域扩展，递归状态演化机制实现间接无限深度图遍历而不线性增加延迟，并采用轨迹对齐训练和图硬负采样。在十亿级工业数据集上，IID-Nav超越主流基线，显著缓解搜索漂移，保持高精度，为工业推荐系统提供高效鲁棒方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29970v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1740, \"height\": 1000, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29970v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 749, \"height\": 538, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29970v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1475, \"height\": 449, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29970v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 943, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29970v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 790, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29970v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 838, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29970v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 846, \"height\": 220, \"label\": \"Table\"}]"
motivation: 现有检索方法陷入兴趣隧道或搜索漂移，无法跟踪实时用户意图，需突破静态入口和固定图拓扑限制。
method: IID-Nav包括目标感知导航策略、递归状态演化支持间接无限深度遍历、轨迹对齐训练及其图硬负采样优化。
result: 在十亿级工业数据集上，IID-Nav在严格延迟预算下超越主流基线，有效缓解搜索漂移并保持深度检索路径高精度。
conclusion: IID-Nav为工业推荐系统提供高效、鲁棒的检索解决方案。
---

## 摘要
现代大规模推荐检索正从静态相似性匹配向动态物品空间导航转变，将检索视为迭代的目标驱动图遍历。传统物品到物品（i2i）方法陷入“兴趣隧道”，无法挖掘用户深层兴趣，而现有的基于索引的检索因静态入口节点和固定图拓扑无法跟踪实时用户意图的转移，导致持续的“搜索漂移”。为解决上述缺陷，我们提出IID-Nav框架，将检索建模为有状态的自主图探索，核心贡献包括三点：（1）目标感知导航策略，以主动意图路由替代被动邻域扩展，并由目标判别器监督；（2）递归状态演变机制，通过跨请求状态复用支持间接无限深度（IID），实现逻辑无限深度图遍历而不线性增加推理延迟；（3）轨迹对齐训练范式，配备图困难负采样，以稳定完整导航路径上的优化。在十亿级工业数据集上的评估表明，IID-Nav在严格延迟预算下超越了主流检索基线。实验结果验证了我们的方法显著缓解了搜索漂移，并在深度检索路径上保持高精度，为工业推荐系统提供了高效、鲁棒的检索解决方案。

## Abstract
Modern large-scale recommender retrieval is shifting from static similarity matching to dynamic item space navigation, framing retrieval as iterative goal-driven graph traversal. Conventional item-to-item (i2i) methods fall into the "interest tunnel" and fail to excavate deep user interests, while existing index-based retrieval suffers from persistent "search drift", caused by static entry nodes and fixed graph topologies unable to track shifting real-time user intent. To resolve the above defects, we present IID-Nav, a framework modeling retrieval as stateful autonomous graph exploration with three core contributions: (1) A goal-aware navigation policy substituting passive neighborhood expansion with active intent routing supervised by a target discriminator; (2) A recursive state evolution mechanism supporting Indirectly Infinite Depth (IID) via cross-request state reuse, which enables logical unlimited-depth graph traversal without linearly rising inference latency; (3) A trajectory-aligned training paradigm equipped with graph hard negative sampling to stabilize optimization over full navigation paths. Evaluations on billion-level industrial datasets show IID-Nav surpasses mainstream retrieval baselines under strict latency budgets. Empirical results verify that our method alleviates search drift remarkably and retains high precision for deep retrieval paths, offering an efficient, robust retrieval solution for industrial recommendation systems.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将严格按照您的要求，对论文《From Extraction to Navigation: Progressive Retrieval with Indirectly Infinite Depth》进行结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大规模推荐系统中的检索阶段面临两大挑战。首先是“**兴趣隧道（Interest Tunnel）**”效应：传统基于物品（i2i）的静态检索方法受限于预定义的、局部的图结构，无法挖掘用户深层次的、位于局部行为簇之外的潜在兴趣。其次是“**搜索漂移（Search Drift）**”：现有的结构化索引（如树、图）依赖于静态的全局入口点，当搜索深度增加时，轨迹容易偏离用户实时的真实意图，导致召回率下降。

- **研究动机**：作者认为，检索应从静态的、被动的相似度匹配，演变为一个目标导向的、动态的、在物品空间中进行主动导航的过程。核心瓶颈在于**单个请求的延迟预算**限制了探索的深度。

- **整体含义**：论文提出了一种全新的范式，将检索形式化为一个**有状态的、自主的探索过程**。通过引入“间接无限深度（IID）”理论，打破了单次请求的物理深度限制，逻辑上允许系统跨多次请求累积探索深度，从而发现传统方法无法触及的深层兴趣区域。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：构建一个名为 **IID-Nav** 的导航框架，将检索视为一次状态化的图遍历。该系统由三个关键创新组成：
    1.  **目标感知导航策略**：用一个目标感知的判别器（Ranking Model）来主动引导搜索路径，替代了传统的被动邻域扩展。
    2.  **递归状态演化机制**：通过跨请求的状态持久化（State Relay），实现“间接无限深度（IID）”。即，本次搜索的终点（高价值节点）会作为下一次搜索的起点之一，从而在不线性增加单次请求延迟的情况下，逻辑上实现任意深度的探索。
    3.  **轨迹对齐学习**：设计一种包含图硬负采样（Graph-Hard Negative Sampling）的训练方案，使模型能在长导航路径上保持对用户真实意图的精准对齐。

- **关键技术细节**：
    - **异构导航环境**：构建了两个互补的图结构。
        - **协同图（Collaborative Graph, G_c）**：基于Swing算法，从用户行为中构建物品间的共现关系。
        - **语义图（Semantic Graph, G_s）**：使用多模态大模型（LLM）提取物品特征，构建语义相似度图。
    - **在线导航流程**：
        1.  **初始化**：使用“动态锚点唤醒（Dynamic Anchor Awakening）”，融合用户的**近期兴趣 (Recent Interest, L_u)** 和**缺失记忆兴趣 (Missing-memory Interest, M_u)**，为每个用户生成个性化的初始入口点集。
        2.  **多跳遍历**：以固定的**H=2跳**进行迭代。每跳中，从当前触发集出发，在G_c和G_s中扩展候选集，然后用目标感知判别器对这些候选进行打分，选出Top-K作为下一跳的触发集。
        3.  **IID实现**：本轮检索最终得到的Top-K结果会存入缓存（如 Redis），并在下次请求时与新的初始触发点合并，共同作为搜索起点。这就实现了跨请求的状态传递和探索深度的累积。
    - **目标感知判别器架构**：采用目标注意力（Target Attention）机制，以目标物品的嵌入为Q，历史行为序列的嵌入为K、V，通过交叉注意力计算得到用户当前关于该目标的表征，然后与目标特征一起送入MLP进行评分。模型不局限于双塔结构，可以灵活使用深度交叉特征。
    - **轨迹对齐训练目标**：
        - **负采样策略**：从检索图（G_c, G_s）中采样与正样本在拓扑上靠近但交互上负相关的“**图硬负样本**”。
        - **损失函数**：结合了 InfoNCE 损失和边际排序损失，其中边际损失对不同类型的负样本（如随机负样本、图硬负样本）施加不同的边际（margin），以实现更精细的区分。

### 3. 实验设计：数据集、基准方法、对比方法

- **数据集**：
    - **MovieLens-20M**：电影推荐，公开数据集。
    - **Taobao UserBehavior**：电商推荐，公开数据集。
    - **ShortVideo-Ind**：自建的十亿级工业短视频平台数据集，代表大规模工业场景。

- **基准方法（Baselines）**：
    - **DSSM**：经典双塔模型，使用FAISS进行检索。
    - **Kuaiformer**：基于Transformer的双塔模型，使用FAISS检索。
    - **TDM (Tree-based Deep Model)**：基于树结构的深度检索模型。
    - **NANN (Neural Approximate Nearest Neighbor)**：基于图的神经引导遍历方法，但以无状态方式运行。
    - **Streaming VQ**：结合向量量化和图遍历的方法。

- **评估指标**：
    - **离线**：Recall@K 和 NDCG@K (K=50, 500)，以及系统效率指标 **QPS**（在100ms延迟约束下）。
    - **在线A/B测试**：业务指标，包括**总App使用时长**、**人均使用时长**和**视频观看时长**。

### 4. 资源与算力

- 论文中**未明确说明**所使用的GPU型号、数量和具体的训练时长。这是一点需要指出的信息缺失。

### 5. 实验数量与充分性

- **实验数量**：
    - **整体性能对比（RQ1）**：在3个数据集上与5个基线模型对比，结果汇总于Table 1。
    - **深度探索分析（RQ2 & RQ3）**：通过图2展示了不同跳数（H）和有无IID机制的Recall和QPS变化，验证了IID的效率和效果。
    - **消融研究（RQ4）**：
        - **Table 3**：对两个图（协同图、语义图）和训练策略（图硬负采样）进行消融。
        - **Table 4**：对锚点（Anchor）机制（近期兴趣、缺失记忆兴趣、随机、热门）进行消融。
    - **效率与在线实验（RQ5）**：Table 1有QPS对比，Table 5展示了在线A/B测试的结果。

- **充分性与客观性**：
    - **公平性**：对比了多种代表性范式（双塔、树、图、VQ），基线选择合理。TDM仅在工业数据集上对比，但作者给出了理由（部署复杂性）。
    - **充分性**：验证了核心假设（IID的深度有效性、图融合、锚机制），并进行了在线验证。实验设计较为充分和客观。
    - **不足**：缺乏与最新Transformer/长序列模型的直接对比（虽然列出了SASRec、BERT4Rec等作为相关工作，但未作为基线），消融实验中未分析不同跳数下IID的表现。

### 6. 论文的主要结论与发现

1.  **IID-Nav显著优于现有方法**：在所有数据集上，IID-Nav在召回率（Recall@K）上大幅领先所有基线，尤其在工业数据集上提升显著（Recall@500 +36%）。
2.  **IID机制是性能提升的核心**：结合IID的2跳遍历（H=2）效果优于无IID的4跳，且QPS更高（910 vs 384）。这说明IID成功地在低延迟下模拟了近似无限深的探索。
3.  **图硬负采样有效缓解搜索漂移**：移除图硬负采样导致Recall@500下降8.26%，证实了该策略对在长路径上保持意图对齐、抑制漂移至关重要。
4.  **多图融合和智能锚点不可或缺**：同时使用协同图和语义图，以及融合近期和缺失记忆兴趣的动态锚点，均有显著的性能增益。
5.  **工业生产可行**：在在线A/B测试中，该框架提升了用户总使用时长和视频观看时长等关键业务指标，且QPS与高度优化的双塔系统（DSSM）接近。

### 7. 优点：方法或实验设计上的亮点

- **理论创新点（IID）**：提出“间接无限深度”这一新颖概念，巧妙地解决了检索中深度与延迟的根本矛盾，是本文最大的亮点。
- **方法设计精妙且实用**：将IID实现为状态传递（State Relay），在工程上简单高效；动态锚点设计融合了短期和长期兴趣；图硬负采样直接针对导航场景中的难点。
- **实验设计严谨且覆盖全面**：从离线性能、深度分析、消融研究到大规模在线A/B测试，论证链条完整，结果可信度高。
- **效率与效果兼顾**：论文不只关注模型性能，还重点强调了QPS等工业部署至关重要的工程指标，体现了方法的实用性。

### 8. 不足与局限

- **算力资源未披露**：未报告训练所需的GPU型号、数量和时长，降低了实验的可复现性。
- **基线和实验范围有局限**：
    - 未与最新的基于LLM的检索模型或更先进的序列模型进行直接对比。
    - 缺乏对不同延迟预算下（如50ms, 200ms）系统性能的鲁棒性分析。
    - 未讨论IID状态缓存对用户隐私的潜在影响，以及缓存策略的进一步优化。
- **消融研究的细节不足**：未探究在不同跳数（如H=1, H=3）下，IID机制和轨迹对齐学习的具体贡献度是否有差异。
- **泛化性验证有限**：所有实验基于一个工业平台（短视频）和两个公开数据集。结论的普适性有待在更多领域（如新闻、电商的完全不同架构）中验证。

（完）
