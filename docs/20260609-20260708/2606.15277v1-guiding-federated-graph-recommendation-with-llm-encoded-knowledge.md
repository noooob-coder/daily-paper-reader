---
title: Guiding Federated Graph Recommendation with LLM-encoded knowledge
title_zh: 利用大语言模型编码知识指导联邦图推荐
authors: "Thi Minh Chau Nguyen, Hien Trang Nguyen, Duc Anh Nguyen, Van Ho-Long, Thanh Trung Huynh, Zhao Ren"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15277v1"
tags: ["query:recsys"]
score: 9.0
evidence: 利用LLM知识指导联邦图推荐
tldr: 联邦图推荐面临跨客户端结构表示因数据非独立同分布而不一致的问题，现有方法仅依赖结构聚合而忽略全局语义。本文提出利用LLM编码知识引导聚合的框架，客户端在本地学习结构表示并通过冻结LLM生成语义向量，服务器根据语义相似度指导选择性聚合。在多个标准基准上，推荐准确率持续优于现有联邦图方法。该工作首次将LLM语义知识融入联邦图推荐，实现隐私保护下的语义引导跨客户端协作，具有重要的理论与应用价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15277v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15277v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15277v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1257, \"height\": 560, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15277v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1224, \"height\": 522, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15277v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1230, \"height\": 711, \"label\": \"Table\"}]"
motivation: 联邦图推荐中跨客户端结构表示不一致，现有方法仅依赖结构聚合而忽略全局语义，导致聚合效果差。
method: 客户端同时学习本地结构表示和利用冻结LLM生成语义向量，服务器基于语义相似度选择性聚合结构表示。
result: 在多个标准基准上，推荐准确率持续优于现有联邦图基线，验证了语义引导的有效性。
conclusion: 首次将LLM语义知识融入联邦图推荐，实现了隐私保护下的语义引导跨客户端协作。
---

## 摘要
基于图的推荐系统在从用户-物品交互中提取协同信号方面非常有效，联邦学习（FL）允许在保护用户隐私的同时训练这些模型。然而，跨分布式、非独立同分布客户端聚合图表示仍然是一个挑战；本地学习的结构嵌入往往不对齐，简单的平均无法捕捉有意义的跨客户端关系。大多数现有的联邦图方法仅依赖结构聚合，忽略了大型语言模型（LLM）中丰富的全局语义上下文。在本文中，我们提出了一种新颖的框架，利用LLM编码的知识来指导联邦图推荐。具体来说，客户端从局部图中学习结构表示，同时通过冻结的LLM将其典型交互模式总结为紧凑的语义向量。中央服务器随后利用这些LLM编码的语义信号发现各客户端之间相关的偏好模式，指导其结构表示的选择性聚合。这实现了在语义信息指导下的跨客户端协作，而无需暴露原始数据。在标准基准上的大量实验表明，利用LLM编码知识指导结构对齐在推荐准确性上持续优于现有的联邦图基线。

## Abstract
Graph-based recommender systems are highly effective at extracting collaborative signals from user--item interactions, and federated learning (FL) allows these models to be trained while preserving user privacy. However, aggregating graph representations across distributed, non-IID clients remains a challenge; structural embeddings learned locally often misalign, and naive averaging fails to capture meaningful cross-client relationships. Most existing federated graph methods rely exclusively on structural aggregation, neglecting the rich, global semantic context available in large language models (LLMs). In this paper, we propose a novel framework that uses LLM-encoded knowledge to guide federated graph recommendation. Specifically, clients learn structural representations from local graphs while simultaneously summarizing their typical interaction patterns into compact semantic vectors via a frozen LLM. The central server then uses these LLM-encoded semantic signals to discover related preference patterns across clients, guiding the selective aggregation of their structural representations. This enables semantically informed cross-client collaboration without exposing raw data. Extensive experiments on standard benchmarks show that guiding structural alignment with LLM-encoded knowledge consistently improves recommendation accuracy over existing federated graph baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）
- **核心问题**：联邦图推荐中，客户端数据呈现高度非独立同分布（non-IID），导致跨客户端的结构嵌入不对齐。现有联邦图方法仅依赖结构参数聚合（如简单平均或图级合并），忽略了全局语义上下文，导致聚合效果差。
- **整体含义**：本文首次提出利用大型语言模型（LLM）编码的语义知识来指导结构表示的对齐与合并，从而实现隐私保护下语义优先的跨客户端协作。

## 2. 方法论
- **核心思想**：客户端同时学习局部图结构表示和社区级别的语义描述；服务器根据语义相似度选择性合并结构原型，避免盲目聚合。
- **关键技术细节**：
    - **局部图表示学习**：使用 LightGCN 风格编码器，对本地用户-物品二部图进行 Lg 层传播，最终嵌入为各层平均。
    - **自适应社区抽象**：对用户嵌入使用 HDBSCAN 自动聚类（数量自适应），每个社区计算结构原型 P（用户嵌入均值）和语义描述 S（通过冻结 LLM 编码社区内高频物品的文本元数据）。
    - **语义引导的合并**：服务器先通过加权平均聚合物品嵌入；然后计算跨社区语义相似度 s (cosine) 和结构相似度 r，结合为 q = s * (1 + λ*r)。若 q > δ，则合并社区，生成全局原型。
    - **对齐感知训练**：每个客户端的损失函数 = BPR 排序损失 + λ_align * ||P - P̃||²，鼓励本地结构与全局对齐原型保持一致。
- **算法流程**：客户端侧：1) 局部训练 LightGCN 优化 BPR 损失 → 2) HDBSCAN 聚类 → 3) 计算每个社区的结构原型和 LLM 语义嵌入 → 4) 上传稀疏物品梯度和社区描述符 → 5) 接收合并原型后，对齐损失微调。服务器侧：1) 行级物品聚合 → 2) 构建语义优先社区图 → 3) 识别连通分量并合并原型 → 4) 广播。

## 3. 实验设计
- **数据集**：MovieLens-100K（943用户，1682物品），MovieLens-1M（6040用户，3952物品），Amazon Video（稀疏，含文本元数据）。
- **场景**：联邦学习设置，用户划分到 K 个客户端。**非 IID 模拟**：使用 Dirichlet 分布（α = 0.1, 0.3, 1.0），α 越小异构越严重。
- **Benchmark 指标**：Leave-one-out 测试，HR@10 和 NDCG@10（百分比）。
- **对比方法**：
    - 集中式：MF, NCF, LightGCN
    - 联邦非图：FedMF, FedNCF, PFedRec, FedRecon, MetaMF
    - 联邦图：FedLightGCN, GPFedRec, GFed-PP, UFGraphFR
- **实现细节**：嵌入维度 d=64，图传播层 Lg=2，Adam 学习率 1e-3，每轮 5 个本地 epoch，HDBSCAN 最小簇大小 10，代表物品数 L=10，LLM 冻结（避免同步开销）。

## 4. 资源与算力
- 论文**未明确说明**使用的 GPU 型号、数量或总训练时长。
- 仅提及嵌入维度 64、网络轻量（LightGCN）、LLM 冻结，客户端计算开销主要来自轻量 GNN 和社区聚类，服务器处理社区描述符。通信开销上界为每客户端更新的物品数和社区数。
- 实验推断可在单 GPU（如 V100 或 A100）上完成，但具体细节缺失。

## 5. 实验数量与充分性
- **实验数量**：
    - 表1：三个数据集在 α=0.3 下对比15+方法，报告 HR@10 和 NDCG@10。
    - 图2：三个数据集在 α=1.0, 0.3, 0.1 下的 HR@10 变化（共9个点）。
    - 图3：消融研究（移除语义引导、移除对齐损失、移除结构调制），三个数据集。
    - 图4：超参数 λ 和 λ_align 敏感性（MovieLens-100K）。
- **充分性与公平性**：
    - 覆盖多个常见基准数据集和非 IID 程度。
    - 对比方法包括集中式、联邦非图、联邦图最先进方法（UFGraphFR, GFed-PP 等）。
    - 消融验证各组件贡献，超参数分析证明稳定性。
    - 但**缺少**对不同客户端数量 K 的规模实验、缺少通信成本与运行时间对比、缺少真实异构网络场景。

## 6. 主要结论与发现
- SemFGRec 在所有数据集和所有非 IID 程度下，HR@10 和 NDCG@10 均一致优于最强基线（如 UFGraphFR），绝对提升约 2 个百分点。
- 语义引导的合并是最大贡献组件（消融中移除后性能下降最大）。
- 方法在非 IID 极端情形（α=0.1）下降幅最小，稳定性显著优于对比方法。
- 超参数 λ 和 λ_align 在较宽范围内性能稳定，易于调优。

## 7. 优点
- **创新性**：首次将 LLM 语义知识显式用于指导联邦图推荐的结构对齐，而非仅用于局部增强。
- **隐私友好**：仅上传社区级原型和稀疏物品梯度，不暴露原始交互或用户嵌入，且 LLM 冻结避免额外通信。
- **非 IID 鲁棒性**：语义优先合并机制防止不同语义但结构相似社区的错误合并，显著提升异构分布下的表现。
- **自适应性**：HDBSCAN 自动确定社区数量，适应不同客户端的数据密度与多样性，没有人工预设硬聚类数。
- **实验全面**：包含多个基准、多种异构程度、充分消融与超参数分析。

## 8. 不足与局限
- **未报告系统级开销**：缺乏通信轮数、每轮传输量、GPU 时间、显存占用等实际部署关键指标。
- **实验覆盖有限**：只测试了三个数据集（两个 MovieLens 较小，Amazon Video 规模也有限），缺少大规模真实联邦场景验证（如百万级用户、千级客户端）。
- **LLM 依赖文本元数据**：要求每个物品含有可用的文本描述，若交互数据仅含 ID，则语义信息不可得，方法适用性受限。
- **LLM 冻结未微调**：社区级语义描述高度依赖冻结 LLM 的编码质量，可能无法捕捉领域专用语义；且未讨论不同 LLM 选择的影响。
- **潜在偏差**：语义相似度基于 LLM 编码，可能存在模型偏见或对稀疏数据的不鲁棒；未进行 LLM 编码质量分析或不同 LLM 对比。
- **缺乏公平性评估**：未讨论隐私预算（如差分隐私）、数据异构的真实分布来源、模型在少数群体或多语言上的表现。

（完）
